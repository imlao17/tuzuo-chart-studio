# 定时任务单 · 可读性专项第二轮 + 全功能回归（5 时段接力）

拟定日期：2026-09-12 晚。五个时段（23:10 / 00:10 / 01:10 / 02:10 / 03:10）各触发一次，通过本文档状态表接力。全程**不推送远程**。

## 背景

第一轮可读性专项（9/12 白天，提交 f7a52b7 + 5cfde0d）修复了透明底导出贴深色背景时全部文字隐形的系统性问题（textOnDark 模式）。本轮按用户要求做 5 个时段的深度排查：场景矩阵两轮 + textOnDark 回归 + 共存验证 + 终验汇总。

## 状态表（每段结束必须更新并提交）

| 时段 | 时间 | 范围 | 状态 | 发现/修复 | 提交 |
|---|---|---|---|---|---|
| 1 | 23:10 | 场景矩阵 I：超长类目名 + 旋转轴标签 | 待开始 | — | — |
| 2 | 00:10 | 场景矩阵 II：最小画布 + 极端数据 + 图例遮挡 | 待开始 | — | — |
| 3 | 01:10 | textOnDark 墨色矩阵回归 + 导出验证 | 待开始 | — | — |
| 4 | 02:10 | 全功能共存验证（水印/标注/云端 + 浏览器实测） | 待开始 | — | — |
| 5 | 03:10 | 3 遍全量测试 + 浏览器抽查 + 最终报告 | 待开始 | — | — |

## 时段 1 · 场景矩阵 I：超长类目名 + 旋转轴标签

用 SSR 渲染脚本（做法参照第一轮 clarity 审计；脚本放 /tmp 不入库）对全部 100 模板跑：
1. **超长类目名**（构造 20 字中文类目数据）——轴标签、图例、数据标签的截断/溢出/互相压盖
2. **旋转轴标签**（axisLabelRotation 45°）——与画布边缘/相邻标签的碰撞

能安全自动处理的就地修复 + 行为测试锁定（如 axisLabel overflow 截断、图例 formatter 省略）；涉及产品取舍的（如换行策略）记录留给用户。

## 时段 2 · 场景矩阵 II：最小画布 + 极端数据 + 图例遮挡

1. **最小画布 320×240**——标签拥挤、轴标签互叠、标题占比过大、图例挤压绘图区
2. **极端数据**——单行、60+ 行类目、全零值、超大数值（1e12）、负值为主的模板
3. **图例遮挡**——图例在左/右/上/下四位置时是否压住图形或标签

同上：修复+测试，或记录。

## 时段 3 · textOnDark 墨色矩阵回归 + 导出验证

1. **墨色矩阵**：{透明/白底} × {textOnDark 开/关} × {用户自定义色有无}，抽查 8 个代表模板（折线/分组条形/饼/热力/地图/树图/大数字卡/桑基），断言全局文字/轴标签/数据标签/副标题符合优先级（用户色 > textOnDark > 主题色）。
2. **导出验证**：textOnDark 开启时导出 SVG 的浅色墨水占比；PNG 导出路径不报错；白底模式下开关不影响（墨色仍为深色）。

## 时段 4 · 全功能共存验证（浏览器实测）

1. textOnDark + 水印 + 标注三者共存（视觉互不干扰、导出包含）
2. 云端保存往返：开 textOnDark 保存 → 退出/刷新 → 重新打开 → 模式保持
3. 关键路径实测：登录→云端存取、标注添加/删除、水印上传、CSV URL 导入（先重建 /tmp/test-import.csv 并在 4999 起本地服务）、5 家族切换出图

## 时段 5 · 三遍全量 + 浏览器抽查 + 最终报告

1. 连续 3 遍 typecheck + npm test + lint，记录每遍结果。
2. 浏览器抽查一轮完整关键路径（同时段 4 的路径清单）。
3. 五段结果汇总写入「最终报告」节（各段发现与处置、测试结果、推送建议），状态表全部置「完成」。

## 通用红线

- 修复必须带测试，`npm test` 全绿（当前 134 项）+ `npm run lint` 0 输出才提交；提交信息 "Clarity: …" 风格
- 只 git add 触碰的具体文件；**绝不 git push**
- 并发锁：/tmp/tuzuo-review.lock（内容=时段号），开始检查/结束删除；被占则等 10 分钟后跳过并说明
- 工作区可能存在无关的 c2b.db* 文件——不要触碰、不要提交
- dev 服务器：`NEXT_PUBLIC_TUZUO_REQUIRE_AUTH=true nohup npx vinext dev -p 4174 > /tmp/tuzuo-dev.log 2>&1 &`，curl 用 http://localhost:4174（IPv6）；账号 API 仅 dev 模式有 D1；测试账号 night-ui@example.com / NightUi-789（失效则注册新测试号，dev 模式验证链接直接返回）
- 浏览器自动化：优先 tab.playwright.evaluate 页面内 .click()；输入用 value setter + dispatchEvent("input")；操作前等 2-3 秒避恢复竞态
- 用户可见文案简体中文

## 最终报告（时段 5 填写）

（待时段 5 完成）
