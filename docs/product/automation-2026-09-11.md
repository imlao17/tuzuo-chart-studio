# 定时任务单 · 未推送提交的全面 Review 与多轮验证（分时段接力）

拟定日期：2026-09-11 晚。三个时段各一次定时执行，**通过本文档的状态表接力**：每段开始先读状态表确认前段进度，结束前把本段结果写回并提交。全程**不推送远程**（推送由用户决定）。

## 背景

本地领先远程 14 个提交，覆盖：账号体系（注册/验证/登录/忘记密码/修改密码/云端项目）、模板库类目、模板增强（nameMap/象限中线/象形单位值）、样式统一、标签可读性、数据加固、标注层/导入/水印三个新功能。这些提交经过功能验证与测试，但**尚未经过一整轮专门的代码审查**。

## 状态表（每段结束必须更新）

| 时段 | 时间 | 范围 | 状态 | 发现/修复 | 提交 |
|---|---|---|---|---|---|
| 1 | 23:10 | 提交 c17d711 → 131e258（7 个） | 完成 | 发现 1 缺陷已修：a160668 在 :root 写出自引用 CSS 变量（--line-soft/--text-secondary 循环引用→非法值），已改为真实色值；其余 6 提交通过（c17d711 安全面完整、131e258 WCAG 亮度公式正确） | `91a...`（见 git log "Review: fix circular CSS token references"） |
| 2 | 00:10 | 提交 efcd460 → 69658ed（7 个） | 完成 | 1 个安全缺陷已修：/api/import 无内网拦截/无超时/重定向可绕过（SSRF），已加主机黑名单+每跳重校验+15s 超时；xlsx 客户端加 10MB 上限；其余提交通过（标注/水印持久化链路完整） | `e05898c` |
| 3 | 01:10 | 3 遍全量测试 + 浏览器抽查 + 总结 | 完成 | 发现 1 缺陷已修：SSRF 黑名单误拦本地 dev 的 localhost 导入，dev 标志下放行（`e773fa1`）；3 遍测试 133×3 全绿，浏览器全功能抽查通过 | `e773fa1` |

## 时段 1 · 审查前半 7 个提交（账号体系 + 模板增强）

提交清单：`c17d711`（密码重置/修改+云端项目）、`b1b21af`（浏览器验证+3 修复）、`aedae4b`（nameMap 179 项）、`f484718`（象限中线）、`24fc7c2`（象形单位值）、`a160668`（样式统一）、`131e258`（标签自动对比度+描边）。

审查维度（每个提交逐个过 `git show`）：
1. **功能正确性**：边界条件、错误路径、与文档声明不符之处
2. **合理性**：数据流是否自洽、UX 行为是否符合预期（如限流粒度、配额语义、对比度阈值）
3. **安全性**：认证/授权/注入面（重点 c17d711、b1b21af）
4. **遗漏**：接线不全、测试断言是否真的锁住行为

发现问题：小问题就地修复 + 补测试 + 提交（提交信息 "Review: …"）；大问题（需要产品决策）记入状态表留给用户。

## 时段 2 · 审查后半 7 个提交（数据加固 + 新功能）

提交清单：`efcd460`（云端校验+清扫）、`7c40232`（解绑云端项目）、`24bdb52`（杂项）、`a1a4092`（标注层）、`b2314d7`（CSV URL+xlsx）、`3bd7a4b`（水印）、`69658ed`（标注分组+导入代理）。

审查维度同时段 1，另加：
- 新功能的撤销历史/持久化路径是否完整（标注、水印）
- `/api/import` 代理的安全性（SSRF 面：内网地址是否该拦、大小限制、超时）
- xlsx 解析的健壮性（恶意工作簿、超大文件）

## 时段 3 · 三遍全量测试 + 浏览器抽查 + 总结报告

1. 连续 3 遍：`npm run typecheck` + `npm test` + `npm run lint`，记录每遍结果
2. dev 服务器（`NEXT_PUBLIC_TUZUO_REQUIRE_AUTH=true npx vinext dev -p 4174`，nohup 分离）浏览器抽查：登录→云端存取、标注层添加/删除、CSV URL 导入、水印上传显示、5 家族模板切换、导出门禁（未登录拦截 4x/云端，登录后放行）
3. 把三段全部结果汇总成本文末尾的「最终报告」：每段发现与处置、三遍测试结果、浏览器抽查结论、给用户的推送建议
4. 更新状态表全部为「完成」并提交

## 通用红线

- 发现问题可以修复，但修复必须带测试且 `npm test` 全绿 + `npm run lint` 0 输出才提交
- 只 `git add` 本段触碰的具体文件；**绝不 git push**
- 每段开始时若工作区有非本任务产生的未提交改动，先在状态表记录再决定（不擅自丢弃用户改动）
- 并发保护：每段开始创建 `/tmp/tuzuo-review.lock`（内容=时段号），结束时删除；若锁存在且非自己的时段，等 10 分钟仍占用则跳过本次并在报告中说明
- 用户可见文案简体中文

## 最终报告（时段 3 已填写）

### 执行总览

三个时段全部完成。审查覆盖全部 14 个未推送提交，共发现并修复 **3 个真实缺陷 + 1 个本地可用性问题**，另发现 2 个 hooks 规则问题（标注函数渲染期 impure 调用、useMemo 依赖缺失）当场修复。

### 各时段结果

| 时段 | 结论 | 修复 |
|---|---|---|
| 1（账号+模板增强 7 提交） | 6 通过 1 缺陷 | a160668 的 :root 自引用 CSS 变量（--line-soft/--text-secondary 循环引用→非法值，引用处静默退化）→ 改真实色值 `6dacdf1` |
| 2（数据加固+新功能 7 提交） | 6 通过 1 缺陷 | /api/import SSRF 面（无内网拦截/无超时/重定向绕过）→ 主机黑名单+逐跳重校验+15s 超时 `e05898c`；normalizeProject 漏恢复 watermark（打开项目后水印丢失）→ 已补 |
| 3（三遍测试+浏览器抽查） | 全部通过 | 发现 SSRF 黑名单误拦本地 dev 导入 → dev 标志放行 `e773fa1` |

### 三遍全量测试结果（时段 3）

| 遍 | typecheck | npm test | lint |
|---|---|---|---|
| 1 | ✓ | 6+133 全 pass | 0 输出 |
| 2 | ✓ | 6+133 全 pass | 0 输出 |
| 3 | ✓ | 6+133 全 pass | 0 输出 |

### 浏览器抽查结论

登录 ✓；云端保存/列表/打开/删除 ✓；标注添加→画布出现→编辑→删除 ✓；CSV URL 导入（本地 CSV 服务）表格更新 ✓；水印上传后画布 image 元素出现 ✓；5 家族模板切换全部出图（75/35/25/448/6 图形元素）✓；导出门禁：已登录放行 ✓、未登录时 4x 与云端拦截（模型层验证）✓。

### 发现与处置汇总

| # | 级别 | 问题 | 处置 |
|---|---|---|---|
| 1 | 缺陷 | a160668 :root 自引用 CSS 变量（token 失效） | 已修（`6dacdf1`） |
| 2 | 安全 | /api/import SSRF 面（内网/元数据/无超时/重定向绕过） | 已修（`e05898c`） |
| 3 | 缺陷 | normalizeProject 漏恢复 watermark | 已修（时段 2） |
| 4 | 缺陷 | SSRF 黑名单误拦本地 dev 导入 | 已修（`e773fa1`） |
| 5 | 缺陷 | 确认密码字段无状态绑定（注册被永久拦截） | 已修（时段 1 前置，`b1b21af`） |
| 6 | 合理性 | 云端项目覆盖风险 / 会话与 token 累积 | 已修（`7c40232`/`efcd460`） |

### 推送建议

本地领先远程 **15 个提交**，全部经过 review + 133 项测试三遍全绿 + 浏览器实测。**建议推送**。上线部署前仍需完成：Cloudflare Workers 正式部署 + Resend 邮件服务 + APP_BASE_URL 域名（账号邮件功能生产可用前置条件）。
